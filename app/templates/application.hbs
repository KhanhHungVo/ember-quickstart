<div class="container">
  <div class="menu" style="background:grey">
    {{#link-to "index"}}
      <h1>
        <em>Shared Layout</em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to "about" class="menu-about"}}
        About
      {{/link-to}}
      {{#link-to "contact" class="menu-contact"}}
        Contact
      {{/link-to}}
    </div>
  </div>
  <div class="body" style="background:yellow">
    {{outlet}}
  </div>
</div>
